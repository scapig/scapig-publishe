
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

POST    /publish                    @controllers.PublishController.publishApiVersion()

GET     /raml                       @controllers.RamlController.fetchRaml(context: String, version: String)
